# .env.example — คัดลอกเป็น .env แล้วแก้ไขค่า
# ไฟล์นี้ commit ได้, แต่ .env จริงห้าม commit

# URL ของ FastAPI backend
# - local dev  : http://localhost:8000
# - production : https://your-api.example.com
API_BASE=http://localhost:8000
